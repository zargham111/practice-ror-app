<div class="flex flex-col items-center pt-16">
  <h1 class="font-bold text-4xl">All Available Rides</h1>
  <div class="grid gap-4 mt-8">
    <% @available_rides.each do |ride| %>
      <%= render partial: "available_rides", locals: { ride: ride } %>
    <% end %>
  </div>
</div>
